# Source parsing and PDF capture use the approved Docker toolbox.
# Host dependencies below are for trusted repository instruction checks only.

# YAML frontmatter validation for tools/check_skills.py.
PyYAML>=6.0
